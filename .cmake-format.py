# This configuration should work with cmake-format 0.6.10 and higher.
with section("format"):
  # allow set(VAR VALUE PARENT_SCOPE) to remain on one line
  max_subgroups_hwrap = 3
  #max_pargs_hwrap = 60
